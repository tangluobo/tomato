#!/bin/sh
set -eu

APP_HOME=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
DATA_HOME="${HOME:-/tmp}/.tomato"
mkdir -p "$DATA_HOME"
cd "$DATA_HOME"
export DYLD_LIBRARY_PATH="$APP_HOME${DYLD_LIBRARY_PATH:+:$DYLD_LIBRARY_PATH}"
exec "$APP_HOME/tomato-bin" "$@"
